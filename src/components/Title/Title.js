import React from "react";
import "./Title.css";

const Title = props => (
	<div className="titleblock pt-5 pb-3">
	<h1 className="title mb-0">Korean Alphabet Click</h1>
	<h5 className="subtitle pb-3">Click on images to earn points, but don't click on an image more than once!</h5>
	</div>
	);

export default Title;
